<UserControl x:Class="wpfChangeViewFromCode.view.RedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:wpfChangeViewFromCode.view"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="22"/>
        </Style>
    </UserControl.Resources>
    <Grid Background="Red">
        <TextBox x:Name="tbData" VerticalContentAlignment="Center" HorizontalContentAlignment="Right" Width="300" Height="50" Text="{Binding MyRedString}"></TextBox>
    </Grid>
</UserControl>
